import { cardTitle } from '../main'

const usersPageStyle = theme => ({
	cardTitle: {
		...cardTitle,
		marginTop: '15px',
		marginBottom: '0px'
	},
	icon: {
		marginLeft: 15
	}
})

export default usersPageStyle
